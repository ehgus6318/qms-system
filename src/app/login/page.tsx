import LoginClient from '@/components/auth/LoginClient';

export const metadata = {
  title: 'DMS - 로그인',
  description: '전자문서관리시스템 로그인',
};

export default function LoginPage() {
  return <LoginClient />;
}
